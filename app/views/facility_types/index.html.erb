<div class="facility list"><h2>Maintain Facility Types</h2><%= link_to("Add New Facility Type", :action => 'new', :class => 'action new') %><p><% if @facility_types.count > 0 %><table summary="Facility Type" class=listing">	<ul>	<% @facility_types.each do |type| %>			<li><%= link_to(type.name, {:action => 'edit', :id => type.id}, :class => 'action_edit') %></li>	<% end %></table><% else %><p>No facility types have been defined yet in the database. Click the "Add New Facility Type" link above to add a new facility.<% end %></div>	