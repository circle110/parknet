<%= link_to("Add New Membership Level Two", {:action => 'new'}, :class => 'back-link') %>

<div class="membership levels">

		<h3>Membership Level Two</h3>
<ul>
<% @membership_level_twos.each do |m| %>

	<li><%=  %><%= link_to("#{m.name}", {:action => 'edit', :id => m.id}, :class => 'back-link') %></li>

<% end %>
</ul>
</ div>	