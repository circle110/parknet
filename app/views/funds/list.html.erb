<div class="fund list"><h2>Funds</h2><%= link_to("Add New Fund", {:action => 'new'}, :class => 'add_new_item') %><p><table summary="Fund" class=listing" cellpadding="4">	<tr>		<th>Fund</th>		<th>Name</th>		<th>Active</th>	</tr>	<% @funds.each do |fund| %>			<tr class="fund_list">				<td><%= fund.fund %></td>				<td><%= link_to("#{fund.name}", {:action => 'edit', :id => fund.id}, :class => 'action_edit') %></td>				<td><%= fund.active = 1 ? "✓":"" %></td>		<%# end %>	<% end %></table></div>	